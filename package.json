{
  "author": "Silvio Gay",
  "name": "simple-typescript-express-template",
  "version": "1.0.0",
  "description": "A simple starter typescript template for NodeJS with express framework.",
  "scripts": {
    "start": "node ./dist/server.js",
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "rm -rf ./dist && tsc",
    "start:dev": "nodemon --config nodemon.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cookie-parser": "^1.4.5",
    "express": "^4.17.1",
    "helmet": "^3.22.0",
    "morgan": "^1.10.0",
    "winston": "^3.2.1"
  },
  "devDependencies": {
    "@types/cookie-parser": "^1.4.2",
    "@types/express": "^4.17.4",
    "@types/helmet": "0.0.45",
    "@types/morgan": "^1.9.0",
    "@types/node": "^13.11.0",
    "@types/winston": "^2.4.4",
    "nodemon": "^2.0.2",
    "ts-node": "^8.8.1",
    "tslint": "^6.1.1",
    "typescript": "^3.8.3"
  }
}
